@model demos.Models.ViewModels.Users.ProfileViewModel
@{
    ViewBag.Title = "Profile Page";
}

<h2>My profile</h2>
<br />
<br />
@Html.ActionLink("Edit profile", "Edit", null, new {@class="btn btn-lg btn-primary"})

<h3>Name: @Html.DisplayFor(vm=> vm.Name)</h3>
<h3>Email: @Html.DisplayFor(vm => vm.Email)</h3>
<h3>Birth date: @Html.DisplayFor(vm => vm.BirthDate)</h3>

<table class="table table-striped">
    <tr>
        <th>Name:</th>
        <th>Grade:</th>
    </tr>

    @foreach (var course in Model.EnrolledCourses)
    {
        <tr>
            <td>@Html.DisplayFor(vm => course.Name) </td>
            <td>@Html.DisplayFor(vm => course.Grade) </td>

        </tr>
    }

</table>
@Html.ActionLink("All courses", "Index", "Home")